{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Start all services",
        "type": "shell",
        "command": "docker-compose -f appserver/docker-compose.yml up -d && docker-compose -f straw/docker-compose.yml up -d",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": []
      },
      {
        "label": "Stop all services",
        "type": "shell",
        "command": "docker-compose -f straw/docker-compose.yml down && docker-compose -f appserver/docker-compose.yml down",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": []
      }
    ]
  }
  